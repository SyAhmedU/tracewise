# Optional. Without it, Tracewise generates deterministic follow-up probes
# (the app is fully functional offline). With it, probes are AI-generated.
ANTHROPIC_API_KEY=

# Optional override. Defaults to a fast, cheap model suited to short probes.
TRACEWISE_MODEL=claude-haiku-4-5-20251001
